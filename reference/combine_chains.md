# Combine Chains Function to combine the posterior samples from the multiple chains.

Combine Chains Function to combine the posterior samples from the
multiple chains.

## Usage

``` r
combine_chains(results)
```

## Arguments

- results:

  A list of MCMC chain results.

## Value

A list with combined results, including median, threshold, first
quartile, and asymptote values.
